# Server Environment Configuration
PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:3000

# Security / Authentication
JWT_SECRET=campusmind_super_secret_jwt_key_2026_rag_system
JWT_EXPIRES_IN=7d

# Database (Leave empty or default for local MongoDB / auto in-memory MongoDB)
MONGODB_URI=mongodb://127.0.0.1:27017/campusmind

# AI Providers (Optional - Full fallback offline responder is available if left empty)
OPENROUTER_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=

# Vector Search Thresholds
RAG_SIMILARITY_THRESHOLD=0.72
RAG_TOP_K=5

# Redis (Optional for BullMQ - automatic synchronous queue fallback enabled if omitted)
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=

# Uploads directory
UPLOAD_DIR=uploads
